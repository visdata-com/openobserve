# OpenObserve 本地调试存储配置详解

## 目录

1. [核心问题](#1-核心问题)
2. [存储架构概述](#2-存储架构概述)
3. [两种模式对比](#3-两种模式对比)
4. [配置详解](#4-配置详解)
5. [MySQL 调试时的存储配置](#5-mysql-调试时的存储配置)
6. [常见问题](#6-常见问题)

---

## 1. 核心问题

### 问题 1: 本地调试需要对象存储吗?

**答案取决于您的配置模式:**

| 模式 | 需要对象存储? | 数据存储位置 | 适用场景 |
|------|--------------|-------------|---------|
| **本地模式** (`ZO_LOCAL_MODE=true`) | ❌ **不需要** | 本地磁盘 | 单机开发测试 |
| **集群模式** (`ZO_LOCAL_MODE=false`) | ✅ **需要** | S3/MinIO/GCS 等 | 多节点/MySQL 调试 |

### 问题 2: 如果没有对象存储数据在哪里?

**本地模式下,数据存储在本地文件系统:**

```
<ZO_DATA_DIR>/
├── stream/          # 数据流文件(Parquet)
├── wal/             # 写入前日志(WAL)
├── db/              # 元数据(SQLite)
├── cache/           # 缓存文件
└── tmp/             # 临时文件
```

---

## 2. 存储架构概述

### 2.1 OpenObserve 数据存储分层

OpenObserve 的数据存储分为 **三层**:

```
┌─────────────────────────────────────────────────────────────┐
│                     1. 元数据存储(Meta Store)                 │
│  - 用途: 组织/流/用户/告警等元数据                             │
│  - 本地模式: SQLite(本地文件)                                 │
│  - 集群模式: MySQL/PostgreSQL(共享数据库)                     │
│  - 配置: ZO_META_STORE, ZO_META_MYSQL_DSN                    │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                     2. WAL 日志(Write-Ahead Log)              │
│  - 用途: 数据写入缓冲,防止丢失                                 │
│  - 位置: 始终在本地磁盘                                       │
│  - 路径: ZO_DATA_WAL_DIR(默认 {ZO_DATA_DIR}/wal/)            │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                     3. 数据文件存储(Data Store)                │
│  - 用途: Parquet 格式的持久化数据                             │
│  - 本地模式: 本地磁盘 {ZO_DATA_DIR}/stream/                   │
│  - 集群模式: S3/MinIO/GCS 对象存储                            │
│  - 配置: ZO_LOCAL_MODE_STORAGE, ZO_S3_*                      │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 关键配置参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `ZO_LOCAL_MODE` | `true` | 是否启用本地模式 |
| `ZO_LOCAL_MODE_STORAGE` | `disk` | 本地模式下的存储类型(`disk`/`s3`) |
| `ZO_META_STORE` | `""` | 元数据存储类型(空=SQLite, `mysql`, `postgres`) |
| `ZO_DATA_DIR` | `./data/openobserve/` | 数据根目录 |
| `ZO_DATA_STREAM_DIR` | `{ZO_DATA_DIR}/stream/` | 数据流文件目录 |
| `ZO_S3_BUCKET_NAME` | `""` | S3 桶名称(集群模式必需) |

---

## 3. 两种模式对比

### 3.1 本地模式 (Local Mode)

**配置:**

```bash
ZO_LOCAL_MODE=true                     # 启用本地模式
ZO_LOCAL_MODE_STORAGE=disk             # 使用本地磁盘存储(默认)
# 不需要配置 S3
```

**特点:**

✅ **优点:**
- ✅ 不需要 S3/MinIO 等对象存储
- ✅ 单机即可运行,快速启动
- ✅ 适合开发和快速原型
- ✅ 数据存储在本地,读写速度快

❌ **缺点:**
- ❌ 只能单节点运行(node_role 强制为 `all`)
- ❌ 元数据只能使用 SQLite(无法使用 MySQL)
- ❌ 无法水平扩展
- ❌ 数据与服务器绑定(服务器故障=数据丢失)

**数据存储位置:**

```
./data/openobserve/
├── stream/                    # ← 数据文件(Parquet)
│   └── logs/
│       └── default/
│           └── 2025/
│               └── 01/
│                   └── 29/
│                       └── 16/
│                           └── *.parquet
├── wal/                       # ← WAL 日志
│   └── logs/
│       └── default/
│           └── 0/
│               └── *.wal
├── db/                        # ← SQLite 元数据
│   └── metadata.db
├── cache/                     # ← 缓存
└── tmp/                       # ← 临时文件
```

**代码实现:**

```rust
// src/config/src/config.rs:2234-2235
cfg.common.is_local_storage = cfg.common.local_mode
    && (cfg.common.local_mode_storage == "disk" || cfg.common.local_mode_storage == "local");
```

---

### 3.2 集群模式 (Cluster Mode)

**配置:**

```bash
ZO_LOCAL_MODE=false                    # 禁用本地模式(使用集群模式)
ZO_META_STORE=mysql                    # 使用 MySQL 元数据存储
ZO_META_MYSQL_DSN=mysql://o2_user:o2_password_123@localhost:3306/openobserve

# ⚠️ 必须配置对象存储
ZO_S3_PROVIDER=minio                   # S3 兼容存储提供商
ZO_S3_SERVER_URL=http://localhost:9000 # MinIO 地址
ZO_S3_ACCESS_KEY=minioadmin
ZO_S3_SECRET_KEY=minioadmin
ZO_S3_BUCKET_NAME=openobserve          # 存储桶名称
ZO_S3_REGION_NAME=us-east-1
```

**特点:**

✅ **优点:**
- ✅ 支持多节点水平扩展
- ✅ 可以使用 MySQL/PostgreSQL 共享元数据
- ✅ 数据存储在对象存储(高可用)
- ✅ 节点无状态,可随意增减
- ✅ 数据与服务器解耦

❌ **缺点:**
- ❌ 必须部署对象存储(S3/MinIO/GCS)
- ❌ 配置复杂
- ❌ 网络延迟影响性能
- ❌ 运维成本高

**数据存储位置:**

```
本地文件系统:
./data/openobserve/
├── wal/                       # ← WAL 日志(仍在本地)
└── cache/                     # ← 缓存(本地)

对象存储 (S3/MinIO):
s3://openobserve/
├── files/                     # ← 数据文件(Parquet)
│   └── logs/
│       └── default/
│           └── 2025/
│               └── 01/
│                   └── 29/
│                       └── 16/
│                           └── *.parquet
└── metadata/                  # ← 一些元数据文件

MySQL 数据库:
openobserve.meta                # ← 组织/流/用户等元数据
openobserve.stream_settings     # ← 流配置
openobserve.alerts              # ← 告警规则
...
```

---

## 4. 配置详解

### 4.1 本地模式存储配置

#### 配置项: `ZO_LOCAL_MODE_STORAGE`

| 值 | 说明 | 数据存储位置 |
|----|------|-------------|
| `disk` (默认) | 使用本地磁盘 | `{ZO_DATA_DIR}/stream/` |
| `local` | 同 `disk` | `{ZO_DATA_DIR}/stream/` |
| `s3` | 使用对象存储 | S3/MinIO(需配置 `ZO_S3_*`) |

**代码逻辑:**

```rust
// src/config/src/config.rs:749-751
// ZO_LOCAL_MODE_STORAGE is ignored when ZO_LOCAL_MODE is set to false
#[env_config(name = "ZO_LOCAL_MODE_STORAGE", default = "disk")]
pub local_mode_storage: String,
```

**示例配置:**

```bash
# 方式 1: 使用本地磁盘(默认,推荐)
ZO_LOCAL_MODE=true
ZO_LOCAL_MODE_STORAGE=disk
ZO_DATA_DIR=./data/openobserve/

# 方式 2: 本地模式 + S3 存储(混合模式)
ZO_LOCAL_MODE=true
ZO_LOCAL_MODE_STORAGE=s3
ZO_S3_BUCKET_NAME=my-local-bucket
ZO_S3_ACCESS_KEY=minioadmin
ZO_S3_SECRET_KEY=minioadmin
```

⚠️ **重要提示:**

> 当 `ZO_LOCAL_MODE=false` 时,`ZO_LOCAL_MODE_STORAGE` 参数会被忽略!
> 集群模式强制使用对象存储。

---

### 4.2 数据目录配置

#### 默认目录结构

```rust
// src/config/src/config.rs:2670-2674
if cfg.common.data_stream_dir.is_empty() {
    cfg.common.data_stream_dir = format!("{}stream/", cfg.common.data_dir);
}
```

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `ZO_DATA_DIR` | `./data/openobserve/` | 数据根目录 |
| `ZO_DATA_WAL_DIR` | `{ZO_DATA_DIR}/wal/` | WAL 日志目录 |
| `ZO_DATA_STREAM_DIR` | `{ZO_DATA_DIR}/stream/` | 数据流文件目录 |
| `ZO_DATA_DB_DIR` | `{ZO_DATA_DIR}/db/` | 元数据数据库目录 |
| `ZO_DATA_CACHE_DIR` | `{ZO_DATA_DIR}/cache/` | 缓存目录 |
| `ZO_DATA_TMP_DIR` | `{ZO_DATA_DIR}/tmp/` | 临时文件目录 |

**自定义配置示例:**

```bash
# Windows 示例
ZO_DATA_DIR=D:/openobserve/data/
ZO_DATA_WAL_DIR=E:/fast-ssd/wal/        # 使用 SSD 提升性能
ZO_DATA_STREAM_DIR=D:/openobserve/stream/

# Linux 示例
ZO_DATA_DIR=/var/lib/openobserve/
ZO_DATA_WAL_DIR=/mnt/nvme/wal/          # 使用 NVMe 提升 WAL 性能
```

---

## 5. MySQL 调试时的存储配置

### 5.1 核心问题: MySQL 调试必须配置对象存储吗?

**答案: ✅ 是的,必须配置对象存储!**

**原因:**

```rust
// src/config/src/config.rs:2538-2544
if !cfg.common.local_mode
    && !cfg.common.meta_store.starts_with("mysql")
    && !cfg.common.meta_store.starts_with("postgres")
{
    return Err(anyhow::anyhow!(
        "Meta store only support mysql or postgres in cluster mode."
    ));
}
```

**逻辑链:**

```
使用 MySQL
  ↓
ZO_LOCAL_MODE=false (强制要求)
  ↓
集群模式启用
  ↓
必须使用对象存储(S3/MinIO/GCS)
```

---

### 5.2 推荐配置: MySQL + MinIO 本地调试

#### 步骤 1: 安装 MinIO(本地对象存储)

```powershell
# Windows - 使用 Chocolatey 安装
choco install minio

# 或下载二进制文件
# https://dl.min.io/server/minio/release/windows-amd64/minio.exe

# 启动 MinIO 服务器
minio.exe server D:\minio-data --console-address ":9001"

# 访问控制台
# http://localhost:9001
# 默认用户名: minioadmin
# 默认密码: minioadmin
```

#### 步骤 2: 创建存储桶

```powershell
# 方法 1: 使用 Web 控制台
# 访问 http://localhost:9001
# 登录后点击 "Buckets" -> "Create Bucket"
# 输入名称: openobserve

# 方法 2: 使用 mc 命令行工具
mc alias set local http://localhost:9000 minioadmin minioadmin
mc mb local/openobserve
mc ls local/
```

#### 步骤 3: 配置 OpenObserve

**创建 `.env` 文件:**

```bash
# ========================================
# 元数据配置(MySQL)
# ========================================
ZO_LOCAL_MODE=false
ZO_META_STORE=mysql
ZO_META_MYSQL_DSN=mysql://o2_user:o2_password_123@localhost:3306/openobserve

# ========================================
# 对象存储配置(MinIO)
# ========================================
ZO_S3_PROVIDER=minio
ZO_S3_SERVER_URL=http://localhost:9000
ZO_S3_ACCESS_KEY=minioadmin
ZO_S3_SECRET_KEY=minioadmin
ZO_S3_BUCKET_NAME=openobserve
ZO_S3_REGION_NAME=us-east-1
ZO_S3_FEATURE_FORCE_HOSTED_STYLE=true

# ========================================
# 本地路径配置(WAL 和缓存)
# ========================================
ZO_DATA_DIR=./data/openobserve/
ZO_DATA_WAL_DIR=./data/openobserve/wal/

# ========================================
# 用户配置
# ========================================
ZO_ROOT_USER_EMAIL=admin@example.com
ZO_ROOT_USER_PASSWORD=Complexpass#123

# ========================================
# 其他配置
# ========================================
RUST_LOG=info
```

#### 步骤 4: 启动服务

```powershell
# 1. 确保 MySQL 和 MinIO 都已启动
net start MySQL80
# MinIO 应该在另一个终端运行

# 2. 启动 OpenObserve 后端
cargo run

# 3. 启动前端(另一个终端)
cd web
npm run dev
```

#### 步骤 5: 验证存储配置

```powershell
# 1. 写入测试数据
curl -X POST "http://localhost:5080/api/default/logs" `
  -u "admin@example.com:Complexpass#123" `
  -H "Content-Type: application/json" `
  -d '[{"timestamp": "2025-01-29T16:00:00Z", "message": "test storage"}]'

# 2. 等待几分钟让数据上传到 MinIO

# 3. 检查 MinIO 中的数据
# 访问 http://localhost:9001
# 浏览 openobserve 桶,应该能看到 files/ 目录下的 Parquet 文件

# 4. 检查本地 WAL
dir ./data/openobserve/wal/
# 应该能看到 *.wal 文件

# 5. 检查 MySQL 元数据
mysql -u o2_user -p -e "USE openobserve; SHOW TABLES;"
# 应该能看到 meta、stream_settings 等表
```

---

### 5.3 数据流向(MySQL + MinIO 模式)

```
客户端写入数据
      ↓
OpenObserve Ingester
      ↓
1. WAL 写入(本地磁盘)
      ↓
2. MemTable 写入(内存)
      ↓
3. 元数据写入(MySQL)
      ↓
4. 达到上传条件(5-15分钟)
      ↓
5. 生成 Parquet 文件
      ↓
6. 上传到 MinIO (S3)
      ↓
7. 删除本地 WAL
```

**关键时间点:**

| 事件 | 位置 | 延迟 |
|------|------|------|
| 数据写入 WAL | 本地磁盘 | ~1ms |
| 数据写入 MemTable | 内存 | ~1ms |
| 元数据写入 MySQL | MySQL 数据库 | ~5-10ms |
| Parquet 上传到 MinIO | MinIO S3 | 5-15 分钟 |

---

## 6. 常见问题

### Q1: 本地模式可以使用 MySQL 吗?

**❌ 不可以!**

```rust
// src/config/src/config.rs:2230-2235
if cfg.common.local_mode {
    cfg.common.node_role = "all".to_string();
}
```

**原因:**
- 本地模式 (`ZO_LOCAL_MODE=true`) 强制使用 SQLite
- 想使用 MySQL 必须设置 `ZO_LOCAL_MODE=false`
- 集群模式必须配置对象存储

---

### Q2: 集群模式可以不配置对象存储吗?

**❌ 不可以!**

**原因:**
- 集群模式 (`ZO_LOCAL_MODE=false`) 需要共享存储
- 所有节点必须能访问同一份数据
- 本地磁盘无法跨节点共享
- 必须使用 S3/MinIO/GCS 等对象存储

**错误示例:**

```bash
# ❌ 这样配置会启动失败
ZO_LOCAL_MODE=false
ZO_META_STORE=mysql
# 缺少 ZO_S3_* 配置

# 错误信息:
# "S3 bucket name is empty, you must set it in cluster mode"
```

---

### Q3: 没有 AWS S3,如何本地调试集群模式?

**✅ 使用 MinIO(开源 S3 兼容对象存储)**

MinIO 是 S3 兼容的本地对象存储,完美用于开发调试:

```powershell
# 1. 安装 MinIO
choco install minio

# 2. 启动 MinIO
minio.exe server D:\minio-data --console-address ":9001"

# 3. 配置 OpenObserve 使用 MinIO
ZO_S3_PROVIDER=minio
ZO_S3_SERVER_URL=http://localhost:9000
ZO_S3_ACCESS_KEY=minioadmin
ZO_S3_SECRET_KEY=minioadmin
ZO_S3_BUCKET_NAME=openobserve
```

**其他替代方案:**

| 方案 | 说明 | 适用场景 |
|------|------|---------|
| **MinIO** | S3 兼容,本地部署 | ✅ 推荐用于开发调试 |
| **LocalStack** | AWS 服务模拟器 | 测试 AWS 集成 |
| **Azurite** | Azure Blob 模拟器 | 测试 Azure 集成 |
| **真实 S3** | AWS S3 服务 | 生产环境 |

---

### Q4: WAL 数据会占用多少磁盘空间?

**估算公式:**

```
WAL 磁盘占用 ≈ 数据写入速率 × 上传间隔 × 1.2

示例:
- 写入速率: 10 MB/s
- 上传间隔: 10 分钟(600 秒)
- WAL 占用: 10 MB/s × 600s × 1.2 = 7.2 GB
```

**配置建议:**

```bash
# 调整上传间隔(减少 WAL 占用)
ZO_FILE_PUSH_INTERVAL=5           # 5分钟上传一次(默认 10 分钟)
ZO_MAX_FILE_SIZE_ON_DISK=16       # 16MB 触发上传(默认 32MB)

# 使用独立分区存储 WAL
ZO_DATA_WAL_DIR=/mnt/nvme/wal/    # 使用 NVMe SSD 提升性能
```

---

### Q5: 如何从本地模式迁移到集群模式?

**迁移步骤:**

```powershell
# 1. 备份本地数据
xcopy /E /I ./data/openobserve ./backup-$(Get-Date -Format 'yyyyMMdd')

# 2. 部署 MinIO
minio.exe server D:\minio-data --console-address ":9001"

# 3. 创建 MySQL 数据库
mysql -u root -p -e "CREATE DATABASE openobserve;"

# 4. 修改配置
ZO_LOCAL_MODE=false                              # 启用集群模式
ZO_META_STORE=mysql                              # 使用 MySQL
ZO_META_MYSQL_DSN=mysql://user:pass@localhost:3306/openobserve
ZO_S3_BUCKET_NAME=openobserve                    # 配置 S3

# 5. 迁移数据(使用 OpenObserve 导入工具或手动上传)
# 方式 1: 重新摄取数据(推荐)
# 方式 2: 手动上传 Parquet 文件到 S3

# 6. 启动服务
cargo run
```

---

### Q6: 对象存储的费用问题

**MinIO 本地部署(免费):**
- ✅ 完全免费
- ✅ 无流量费用
- ✅ 无存储限制(仅受磁盘容量限制)

**AWS S3 费用估算:**

| 项目 | 价格(美东) | 示例(100GB/月) |
|------|-----------|---------------|
| 存储 | $0.023/GB/月 | $2.30/月 |
| PUT 请求 | $0.005/1000 次 | ~$0.50/月 |
| GET 请求 | $0.0004/1000 次 | ~$0.10/月 |
| 数据传输(出站) | $0.09/GB | $9.00/月(100GB 出站) |

**建议:**
- 开发测试: 使用 **MinIO**(免费)
- 生产环境: 使用 **S3**/**GCS** + 冷存储层(降低成本)

---

## 7. 最佳实践总结

### 7.1 开发调试配置推荐

#### 场景 1: 纯本地开发(单机,快速启动)

```bash
ZO_LOCAL_MODE=true                     # 本地模式
ZO_LOCAL_MODE_STORAGE=disk             # 本地磁盘
ZO_DATA_DIR=./data/openobserve/        # 数据目录
# 不需要配置 MySQL 或 S3
```

**优点:** 最简单,启动最快
**缺点:** 无法测试多节点功能

---

#### 场景 2: MySQL + MinIO 调试(模拟生产环境)

```bash
# 元数据
ZO_LOCAL_MODE=false
ZO_META_STORE=mysql
ZO_META_MYSQL_DSN=mysql://o2_user:o2_password_123@localhost:3306/openobserve

# 对象存储
ZO_S3_PROVIDER=minio
ZO_S3_SERVER_URL=http://localhost:9000
ZO_S3_ACCESS_KEY=minioadmin
ZO_S3_SECRET_KEY=minioadmin
ZO_S3_BUCKET_NAME=openobserve
ZO_S3_REGION_NAME=us-east-1

# 本地路径
ZO_DATA_DIR=./data/openobserve/
```

**优点:** 最接近生产环境
**缺点:** 需要安装 MySQL 和 MinIO

---

### 7.2 性能优化建议

#### WAL 性能优化

```bash
# 使用 SSD 存储 WAL(关键!)
ZO_DATA_WAL_DIR=/mnt/nvme/wal/

# 调整 fsync 策略
ZO_WAL_FSYNC_INTERVAL=1000             # 1 秒 fsync 一次(默认立即)
```

#### 对象存储性能优化

```bash
# 调整上传参数
ZO_FILE_PUSH_INTERVAL=5                # 5 分钟上传一次
ZO_MAX_FILE_SIZE_ON_DISK=64            # 64MB 触发上传
ZO_FILE_PUSH_LIMIT=10                  # 并发上传 10 个文件

# S3 连接优化
ZO_S3_CONNECT_TIMEOUT=10               # 连接超时 10 秒
ZO_S3_REQUEST_TIMEOUT=3600             # 请求超时 1 小时
ZO_S3_MAX_IDLE_PER_HOST=100            # 连接池大小
```

---

## 8. 故障排查

### 问题 1: "S3 bucket name is empty"

**错误信息:**

```
S3 bucket name is empty, you must set it in cluster mode
```

**原因:** 使用了 `ZO_LOCAL_MODE=false` 但未配置 S3

**解决方案:**

```bash
# 添加 S3 配置
ZO_S3_BUCKET_NAME=openobserve
ZO_S3_ACCESS_KEY=your_access_key
ZO_S3_SECRET_KEY=your_secret_key
```

---

### 问题 2: "Meta store is MySQL, you must set ZO_META_MYSQL_DSN"

**错误信息:**

```
Meta store is MySQL, you must set ZO_META_MYSQL_DSN
```

**原因:** 设置了 `ZO_META_STORE=mysql` 但未提供连接字符串

**解决方案:**

```bash
ZO_META_MYSQL_DSN=mysql://o2_user:o2_password_123@localhost:3306/openobserve
```

---

### 问题 3: MinIO 连接失败

**错误信息:**

```
Error: Connection refused (os error 10061)
```

**排查步骤:**

```powershell
# 1. 确认 MinIO 正在运行
netstat -ano | findstr :9000

# 2. 测试连接
curl http://localhost:9000/minio/health/live

# 3. 检查防火墙
New-NetFirewallRule -DisplayName "MinIO" -Direction Inbound -LocalPort 9000,9001 -Protocol TCP -Action Allow
```

---

## 9. 总结

### 关键要点

| 问题 | 答案 |
|------|------|
| **本地调试需要对象存储吗?** | 取决于模式:<br>- 本地模式(`ZO_LOCAL_MODE=true`): ❌ 不需要<br>- 集群模式(`ZO_LOCAL_MODE=false`): ✅ 需要 |
| **没有对象存储数据在哪里?** | 本地文件系统:<br>- 数据: `{ZO_DATA_DIR}/stream/`<br>- WAL: `{ZO_DATA_DIR}/wal/`<br>- 元数据: `{ZO_DATA_DIR}/db/metadata.db` |
| **MySQL 调试需要对象存储吗?** | ✅ **是的!** MySQL 要求 `ZO_LOCAL_MODE=false`,必须配置 S3/MinIO |
| **推荐的本地对象存储?** | **MinIO** - 免费、S3 兼容、易于安装 |

### 配置速查表

| 场景 | `ZO_LOCAL_MODE` | 元数据存储 | 数据存储 | 是否需要 S3 |
|------|----------------|-----------|---------|------------|
| 纯本地开发 | `true` | SQLite | 本地磁盘 | ❌ 不需要 |
| 本地 + S3 | `true` | SQLite | S3/MinIO | ✅ 需要 |
| MySQL 调试 | `false` | MySQL | S3/MinIO | ✅ 需要 |
| 生产环境 | `false` | PostgreSQL | S3/GCS | ✅ 需要 |

---

**参考文档:**

- [src/config/src/config.rs:745-804](src/config/src/config.rs) - 配置定义
- [src/config/src/config.rs:2230-2235](src/config/src/config.rs) - 本地存储逻辑
- [src/config/src/config.rs:2538-2567](src/config/src/config.rs) - 集群模式校验
- [本地MySQL调试完整指南.md](本地MySQL调试完整指南.md) - MySQL 配置详解

---

**最后更新:** 2025-01-29
**版本:** 1.0
